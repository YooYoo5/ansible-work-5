# ansible n1 -m file -a "path=/opt/devops/ state=directory"
---
- name: Update n1 servers
  hosts: n1

  tasks:
  - name: Create Directory
    file:
      path: /opt/devops/new_dir
      state: directory
...  
